# ⚠️  IMPORTANT: This is a SECRET TEMPLATE — not for production use as-is.
#
# In production, secrets are managed via:
#   - AWS Secrets Manager + External Secrets Operator
#   - Sealed Secrets (Bitnami) for GitOps workflows
#   - Vault Agent Injector (HashiCorp Vault)
#
# The values below are base64-encoded PLACEHOLDERS for local/dev only.
# NEVER commit real secret values to Git.
#
# To generate proper base64 values:
#   echo -n "your-secret-value" | base64
#
# See docs/monitoring-strategy.md for the full secrets management strategy.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: devops-challenge
type: Opaque
data:
  # Placeholder value: "changeme-use-external-secrets-in-prod"
  # Replace with actual value from your secrets manager
  redis-password: Y2hhbmdlbWUtdXNlLWV4dGVybmFsLXNlY3JldHMtaW4tcHJvZA==
