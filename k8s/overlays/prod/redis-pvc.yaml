apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  labels:
    app: redis
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3         # AWS EBS gp3 — change to match your cloud provider
  resources:
    requests:
      storage: 10Gi
